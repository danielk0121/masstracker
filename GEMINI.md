# 프로젝트 작업 사이클 규칙

모든 신규 기능 구현 및 수정 작업은 아래의 사이클을 엄격히 준수한다.

- **브랜치 생성**: 신규 작업을 시작하기 전 `main` 브랜치로 체크아웃하고 `fetch`, `pull`을 수행하여 최신 상태를 유지한 후, `feat/기능명` 또는 `fix/이슈명` 형식으로 신규 브랜치를 생성한다.
- **코드 수정**: 계획된 내용을 바탕으로 코드를 수정하고 기능을 구현한다.
  - **프로토타입 수정 규칙**: `prototype/weight-chart.html`을 수정할 경우, 헤더의 수정 날짜를 현재 한국 시간(KST)으로 반드시 갱신한다. (`TZ='Asia/Seoul' date '+%Y-%m-%d %H:%M:%S'` 명령 사용)
- **검증**: 검증은 생략한다.
- **커밋 및 푸시**: 작업 단위별로 커밋하고 원격 저장소에 푸시한다.
- **PR 생성**: 당분간 PR 을 생성하지 않는다.
- **언어**: 모든 문서는 한국어로 작성한다. (코드 주석, 커밋 내용, PR 내용, md 문서 등)
- **리드미 작업기록**: 리드미 문서 하단에 작업 기록을 했었는데, 이제 기록하지 않는다. (커밋 메세지 참고)

# 백업 사항
- 이 내용은 기록을 남기기 위한 것이며, LLM 이 규칙으로 사용 하지 않는다.
- **PR 생성**: `main` 브랜치를 대상으로 Pull Request를 생성한다. (GitHub CLI `gh` 사용 권장)
- **주의**: 한 사이클당 하나의 PR을 원칙으로 하며, 모든 문서는 한국어로 작성한다.
