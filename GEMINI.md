# 프로젝트 작업 사이클 규칙

모든 신규 기능 구현 및 수정 작업은 아래의 사이클을 엄격히 준수한다.

1. **브랜치 생성**: `feat/기능명` 또는 `fix/이슈명` 형식으로 신규 브랜치를 생성한다.
2. **리드미 업데이트**: `README.md` 파일의 하단에 `---` 줄 구분 기호를 추가한 후 작업할 계획을 먼저 기록한다.
3. **코드 수정**: 계획된 내용을 바탕으로 코드를 수정하고 기능을 구현한다.
4. **검증**: 검증은 생략한다.
5. **커밋 및 푸시**: 작업 단위별로 커밋하고 원격 저장소에 푸시한다.
    - 커밋 이후 리드미 문서에 커밋 해쉬 코드를 작성한다. 그리고 다시 또 커밋 푸시 한다.
6. **PR 생성**: `main` 브랜치를 대상으로 Pull Request를 생성한다. (GitHub CLI `gh` 사용 권장)

*주의: 한 사이클당 하나의 PR을 원칙으로 하며, 모든 문서는 한국어로 작성한다.*
